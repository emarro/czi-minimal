[project]
name = "caduceus"
version = "0.2.0"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["caduceus*"]
exclude = ["tests*"]

