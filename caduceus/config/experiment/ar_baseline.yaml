# @package _global_
# let the experiment node modify configs globally
defaults:
  - override /model: ar
  - override /loggers: wandb

tags: ["ar", "baseline"]



task_name: "ar_baseline"

optimizer:
  lr: 5e-4

loggers:
  wandb:
    tags: ${tags}
